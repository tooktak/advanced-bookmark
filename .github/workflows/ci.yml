name: CI

on:
  push:
    branches: [ master ]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - 
      name: Run eslint
      run: docker run --rm -v $(pwd):/data cytopia/eslint .